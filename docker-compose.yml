services:
  capacitarr:
    build: .
    container_name: capacitarr
    ports:
      - "2187:2187"
    environment:
      - PUID=1001
      - PGID=1001
      - DEBUG=true
    volumes:
      - capacitarr-config:/config
    restart: unless-stopped

volumes:
  capacitarr-config:
