# 1.0.0 Pre-Release Cleanup Plan

**Created:** 2026-03-07T18:20Z
**Status:** 🟡 In Progress (Phase 1 complete)
**Branch:** `chore/1.0.0-pre-release-cleanup`
**Scope:** Code cleanup, feature polish, test coverage, documentation, and final prep for the 1.0.0 release

---

## Context

A comprehensive codebase audit was performed prior to the 1.0.0 release. This plan addresses all findings — every item must be completed before tagging v1.0.0. Items B (make ci) and G (git-cliff changelog) are handled as part of the release process itself, not in this plan.

---

## Phase 1: Make In-App Notifications Always-On ✅

Currently, in-app notifications only work if the user manually creates a notification channel of type `"inapp"` and toggles subscriptions. This is confusing — in-app should be a built-in system feature that is always active. All changes in this phase must strictly follow the service layer architecture: no DB access from route handlers, all business logic in services, proper event publishing, and full test coverage.

### Step 1.1 — Update NotificationDispatchService to always write in-app ✅

**Files:** `backend/internal/services/notification_dispatch.go`

- ✅ In `dispatchDigest()`: After the channel loop, unconditionally call `s.senders["inapp"].SendDigest("", digest)` so every digest is persisted in-app regardless of channel configuration. Log errors but do not fail the overall dispatch.
- ✅ In `dispatchAlert()`: After the channel loop, unconditionally call `s.senders["inapp"].SendAlert("", alert)` so every alert is persisted in-app.
- ✅ Guard with a nil check on the sender (defensive programming — uses map lookup `ok` check).
- ✅ Publish `NotificationSentEvent` for in-app sends with `ChannelType: "inapp"` and appropriate `TriggerType`.

### Step 1.2 — Remove "inapp" as a configurable channel type ✅

**Files:** `backend/internal/db/validation.go`, `backend/routes/notifications.go`

- ✅ Remove `"inapp": true` from `ValidNotificationChannelTypes` in `validation.go`.
- ✅ Update the validation error message in `notifications.go` POST handler: "type must be discord or slack".
- ✅ Update the PUT handler similarly.
- ✅ Removed unused `notifTypeInApp` constant from `routes/notifications.go`.

### Step 1.3 — Add database migration to clean up existing inapp channels ✅

**File:** `backend/internal/db/migrations/00005_remove_inapp_channel_type.sql`

- ✅ Add a new migration that deletes any `notification_configs` rows where `type = 'inapp'`.
- ✅ This is a one-way migration — users won't lose anything since in-app is now automatic.
- **Note:** Used `-- +goose Up` / `-- +goose Down` format (not `-- +migrate Up`) to match existing migration conventions.

### Step 1.4 — Skip inapp channels in dispatch loops ✅

**File:** `backend/internal/services/notification_dispatch.go`

- ✅ In `dispatchDigest()` and `dispatchAlert()`, add a `continue` if `cfg.Type == "inapp"` in the channel loop, since in-app is now handled unconditionally outside the loop. This prevents duplicate in-app notifications if somehow a legacy config still exists.

### Step 1.5 — Update tests ✅

**File:** `backend/internal/services/notification_dispatch_test.go`

- ✅ Add test verifying in-app notifications fire even with zero enabled channels (`TestNotificationDispatch_InAppDigestWithZeroChannels`).
- ✅ Add test verifying in-app notifications fire alongside Discord/Slack channels (`TestNotificationDispatch_InAppDigestAlongsideExternal`).
- ✅ Add test verifying in-app alerts fire with zero enabled channels (`TestNotificationDispatch_InAppAlertWithZeroChannels`).
- ✅ Refactored `newTestDispatch` to use separate mocks for external channels (discord/slack) and in-app, returning both. Updated all 11 existing callers.
- ✅ Tests use canonical "Serenity" media name where applicable.

### Step 1.6 — Frontend: remove inapp from channel type dropdown ✅

**Files:** `frontend/app/components/settings/SettingsNotifications.vue`, `frontend/app/types/api.ts`

- ✅ Removed "In-App" `<UiSelectItem>` from the notification channel creation dropdown.
- ✅ Removed `'inapp'` from the `channelForm.type` type union.
- ✅ Removed `'inapp'` from the `NotificationChannel.type` interface in `types/api.ts`.
- ✅ Removed inapp cases from `channelTypeIcon()`, `channelTypeColor()`, `channelTypeLabel()` helpers.
- ✅ Removed conditional `v-if="channelForm.type !== 'inapp'"` on the webhook URL field.
- ✅ Removed conditional `webhookUrl` assignment in `onChannelSubmit()`.
- ✅ Simplified badge variant (no longer conditionally `'secondary'` for inapp).
- ✅ Updated empty-state description text from "Discord, Slack, or in-app" to "Discord or Slack".
- ✅ The in-app notification listing/read/clear UI is unchanged.

---

## Phase 2: Code Bug Fixes and Hygiene

### Step 2.1 — Fix `sizeMB` → `sizeGB` variable name

**File:** `backend/internal/events/types.go:339`

- Rename `sizeMB` variable to `sizeGB` in `DeletionSuccessEvent.EventMessage()`.

### Step 2.2 — Fix inconsistent URL scheme validation

**File:** `backend/routes/integrations.go:113`

- Replace inline `"http"` and `"https"` strings in the PUT integration handler with `schemeHTTP` and `schemeHTTPS` constants from `constants.go`.

### Step 2.3 — Consolidate duplicate `isMaskedKey()`

**Files:** `backend/routes/integrations.go`, `backend/internal/services/integration.go`

- Remove `isMaskedKey()` from `routes/integrations.go`.
- Keep the copy in `services/integration.go` and export it as `IsMaskedKey()`.
- Update the route handler in `routes/integrations.go` PUT to call `services.IsMaskedKey()` (the routes package already imports `services`).

### Step 2.4 — Fix VersionService direct DB access

**Files:** `backend/internal/services/version.go`, `backend/internal/services/registry.go`

- Add a `PreferencesReader` interface to `version.go`:
  ```go
  type PreferencesReader interface {
      GetPreferences() (db.PreferenceSet, error)
  }
  ```
- Add a `preferences` field to `VersionService` struct.
- Update `NewVersionService()` to accept a `PreferencesReader` parameter.
- Wire it in `Registry.InitVersion()` by passing `r.Settings`.
- Replace the two `s.db.First(&pref, 1)` calls in `CheckForUpdate()` and `ForceCheck()` with `s.preferences.GetPreferences()`.
- Remove the `db *gorm.DB` field from `VersionService` entirely (it should no longer be needed).
- Update `version_test.go` to inject a mock `PreferencesReader`.

### Step 2.5 — Fix inconsistent DELETE response format

**File:** `backend/routes/integrations.go:144`

- Change the DELETE integration response from `{"message": "Integration deleted"}` to `{"status": "deleted"}` to match the pattern used in notification channel DELETE.

### Step 2.6 — Extract inline route handlers from api.go

**Files:** Create new files and update existing ones.

- Create `routes/metrics.go` with `RegisterMetricsRoutes()`:
  - GET `/metrics/history` (moved from api.go)
  - GET `/worker/stats` (moved from api.go)
  - GET `/lifetime-stats` (moved from api.go)
  - GET `/dashboard-stats` (moved from api.go)
- Create `routes/disk_groups.go` with `RegisterDiskGroupRoutes()`:
  - GET `/disk-groups` (moved from api.go)
  - PUT `/disk-groups/:id` (moved from api.go)
- Move POST `/engine/run` into existing `routes/engine_history.go`, renaming it to `routes/engine.go` and renaming the registration function to `RegisterEngineRoutes()`.
- Update `api.go` to call the new registration functions.
- Ensure each new file has the standard route handler pattern: parse request → call service → return response.

### Step 2.7 — Rename `make clean` to prevent accidental volume destruction

**File:** `Makefile`

- Rename the `clean` target to `clean:all` or `clean:volumes`.
- Add a safer `clean` target that only removes containers and build cache (no `-v` flag).
- Update the help text accordingly.

---

## Phase 3: Features and Polish

### Step 3.1 — Add site favicon

**Scope:** Frontend

- Create or source a Capacitarr icon/logo.
- Add to `frontend/public/`:
  - `favicon.ico` (32x32 for legacy browsers)
  - `favicon.svg` (scalable, supports dark mode via CSS media queries)
  - `apple-touch-icon.png` (180x180 for iOS)
  - `site.webmanifest` (PWA metadata with name, icons)
- Update Nuxt config to include appropriate `<link>` tags in `<head>`.

### Step 3.2 — Add Discord embed icon

**File:** `backend/internal/notifications/discord.go`

- Add `IconURL` field to the `discordAuthor` struct: `IconURL string \`json:"icon_url,omitempty"\``
- Set it to a publicly-accessible Capacitarr logo URL (e.g., from the GitLab project avatar or a raw file URL from the repo).
- The same URL can be reused in the Slack sender if desired (Slack Block Kit supports `accessory` images).

### Step 3.3 — Add VersionCheckEvent for activity log

**File:** `backend/internal/events/types.go`

- Add a new `VersionCheckEvent` struct:
  ```go
  type VersionCheckEvent struct {
      CurrentVersion  string `json:"currentVersion"`
      LatestVersion   string `json:"latestVersion"`
      UpdateAvailable bool   `json:"updateAvailable"`
  }
  ```
- Implement `EventType()` → `"version_check"` and `EventMessage()`.

**File:** `backend/internal/services/version.go`

- In `fetchLatestRelease()`, publish `VersionCheckEvent` after every successful check (not just when an update is found).
- Keep the existing `UpdateAvailableEvent` with its fire-once-per-version behavior for notification dispatch.

### Step 3.4 — Add robots.txt

**Scope:** Frontend

- Add `frontend/public/robots.txt` with contents:
  ```
  User-agent: *
  Disallow: /
  ```
- Prevents search engine indexing if accidentally exposed to the internet.

### Step 3.5 — Add security headers middleware

**File:** `backend/main.go`

- Add Echo middleware that sets:
  - `X-Content-Type-Options: nosniff`
  - `X-Frame-Options: DENY`
  - `Referrer-Policy: strict-origin-when-cross-origin`
- Place after the existing middleware stack, before route registration.

### Step 3.6 — Add Cache-Control headers for static assets

**File:** `backend/main.go` (in `serveEmbeddedFile()`)

- For files under `_nuxt/` directory (hashed by Nuxt build): set `Cache-Control: public, max-age=31536000, immutable`.
- For `index.html`, `200.html`, and other non-hashed files: set `Cache-Control: no-cache`.
- This significantly improves frontend load performance on repeat visits.

### Step 3.7 — Add webhook retry with exponential backoff

**Files:** `backend/internal/notifications/discord.go`, `backend/internal/notifications/slack.go`

- Add retry logic to `sendDiscordPayload()` and `sendSlackPayload()`:
  - On HTTP 429 (Too Many Requests): respect `Retry-After` header, wait, then retry.
  - On HTTP 5xx: retry up to 3 times with exponential backoff (1s, 2s, 4s).
  - On other errors: do not retry (fail immediately).
- Extract the retry logic into a shared helper in `notifications/httpclient.go` to avoid duplication.
- Log each retry attempt at `slog.Warn` level.

### Step 3.8 — Add startup self-test for integrations

**File:** `backend/main.go`

- After `pollerInstance.Start()`, run a quick non-blocking connectivity check:
  - Call `reg.Integration.ListEnabled()` to get all enabled integrations.
  - For each, call `reg.Integration.TestConnection()` (which already exists).
  - Log a warning for any that fail — do NOT block startup.
  - This gives immediate feedback in the logs on misconfiguration.
- Run this as a separate goroutine so it doesn't delay the HTTP listener.

### Step 3.9 — Handle semver build metadata in CompareSemver

**File:** `backend/internal/services/version.go`

- Update `splitPrerelease()` to strip build metadata (everything after `+`):
  - `"1.0.0+build.123"` → version `"1.0.0"`, prerelease `""`, build metadata ignored.
  - `"1.0.0-rc.1+build.123"` → version `"1.0.0"`, prerelease `"rc.1"`, build metadata ignored.
- Per SemVer spec, build metadata MUST be ignored when determining version precedence.
- Update `version_test.go` with test cases for build metadata.

---

## Phase 4: Test Coverage

### Step 4.1 — Add notifications route tests

**File:** Create `backend/routes/notifications_test.go`

- Test GET `/notifications/channels` (list).
- Test POST `/notifications/channels` (create with valid/invalid payloads, verify "inapp" type is rejected).
- Test PUT `/notifications/channels/:id` (update with valid/invalid payloads).
- Test DELETE `/notifications/channels/:id`.
- Test POST `/notifications/channels/:id/test` (test notification send).
- Test GET `/notifications` (list in-app).
- Test GET `/notifications/unread-count`.
- Test PUT `/notifications/:id/read` and `/notifications/read-all`.
- Test DELETE `/notifications` (clear all).

### Step 4.2 — Add cron job tests

**File:** Create `backend/internal/jobs/cron_test.go`

- Smoke test that `Start()` returns a valid `*cron.Cron` scheduler.
- Verify the expected number of cron entries are registered (currently 8).
- Test that the scheduler can be stopped cleanly.
- No need to test the actual rollup/prune logic — that's covered by service-level tests.

### Step 4.3 — Add poller loop tests

**File:** Create `backend/internal/poller/poller_test.go`

- Test `getPollInterval()` with various `PollIntervalSeconds` values (including below-minimum).
- Test `safePoll()` recovery — verify that a panicking `poll()` doesn't crash the goroutine.
- Test `Start()`/`Stop()` lifecycle — verify the goroutine starts and stops cleanly.

### Step 4.4 — Sweep test files for canonical media names

**Scope:** All `*_test.go` files

- Search for non-canonical test media names.
- Replace TV show names with "Firefly".
- Replace movie names with "Serenity".
- This applies to test data only — production code (documentation examples, etc.) may use other names.

---

## Phase 5: Documentation

### Step 5.1 — Update architecture.md

**File:** `docs/architecture.md`

- Add `NotificationChannelService`, `NotificationDispatchService`, `DataService`, `RulesService`, `MetricsService`, and `VersionService` to the service layer diagram.
- Add the notification dispatch flow (two-gate flush pattern) as a subsection or reference the notification overhaul plan.
- Update any stale references.

### Step 5.2 — Write notification documentation

**File:** Create `docs/notifications.md`

- Explain the notification architecture: cycle digests vs. instant alerts.
- Document each subscription toggle and what events it covers.
- Document in-app notifications: always-on, automatic, how the notification panel works, pruning behavior.
- Provide step-by-step setup for Discord webhooks.
- Provide step-by-step setup for Slack webhooks.
- Explain the digest content: what data appears, how to read the progress bar, mode-specific formatting.

### Step 5.3 — Add troubleshooting guide

**File:** Create `docs/troubleshooting.md`

- Common issues:
  - Integration connection failures (wrong URL, bad API key, network).
  - Disk groups not appearing (root folder path mismatch, integration not synced).
  - Notifications not sending (webhook URL, channel not enabled, subscription toggles off).
  - Engine not running (poll interval, no integrations enabled).
  - Items not being deleted (dry-run mode, deletions disabled safety guard).
- How to enable debug logging for diagnosis.
- How to check the activity feed for recent events.
- How to verify the startup self-test output.

### Step 5.4 — Add database backup documentation

**File:** Update `docs/deployment.md` (add section)

- Document that `/config/capacitarr.db` is the single source of truth.
- Explain WAL mode and safe backup procedures.
- Provide a simple `cp` or `sqlite3 .backup` example.
- Recommend periodic backups before upgrades.
